テーブル名 : tag_info
要素 : tag_name, tag_id, cross_name
説明 : タグの名前（指定）と所属交差点名（指定）保存
（名称, タグid, 所属交差点名）

テーブル名 : cross_position
要素 : cross_name, x, y
説明 : 交差点の座標保存
（交差点名, x座標, y座標）

テーブル名 : road_connection
要素 : cross_name, x, y
説明 : 交差点の座標保存
（交差点名, x座標, y座標）

テーブル名 : 
要素 : cross_name, car_id, tag_id, destination, status, time
説明 : 交差点での車の状態管理
（交差点名, 車ID, 接続したタグID, 行き先, 車の状態, 時間）
